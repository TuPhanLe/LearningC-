#include <iostream>
#include <string>
#include <cmath>
/* 
    1. Cú pháp (Syntax)
    - Toán tử (Operator): ký hiệu, kí tự -> toán hạng 
    - hàm (function): kí tự ( đối số )
    2. Số toán hạng (operand) tham gia (ngôi) 
    3. Độ ưu tiên (precedence)
    4. Thứ tự thực hiện phép toán (associativity)
    5. Kết quả trả về (return value)
    
*/

int main()
{

}	